inherit_from: config/rubocop.yml

AllCops:
  Exclude:
    - vendor/**/*.rb
    - vendor/bundle/**/*

  NewCops: enable
  TargetRubyVersion: 3.2

plugins:
  - rubocop-md
  - rubocop-rake

Layout/LineLength:
  Exclude:
    - test.rb
